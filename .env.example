# Leave both unset to use browser local storage (no setup needed).
# Set both, then restart the dev server / redeploy, to switch to Supabase.
# See supabase/schema.sql for the table this app expects.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
